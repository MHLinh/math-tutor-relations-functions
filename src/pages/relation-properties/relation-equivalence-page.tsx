import React from "react"
import {
  Container,
} from "@mui/material"
import { RelationEquivalence } from "components"

/**
 * Displays the page for studying the equivalence relation property.
 */
export function RelationEquivalencePage() {
  return (
    <Container>
      <RelationEquivalence />
    </Container>
  )
}
