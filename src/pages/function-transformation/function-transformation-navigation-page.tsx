import React from "react"
import {
  Container,
} from "@mui/material"
import { FunctionTransformationNavigation } from "components"

/**
 * Displays the page with navigation buttons
 * for studying the quadratic function.
 */
export function FunctionTransformationNavigationPage() {
  return (
    <Container>
      <FunctionTransformationNavigation />
    </Container>
  )
}
